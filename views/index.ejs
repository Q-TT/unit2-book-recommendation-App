<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" 
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script defer src="../js/app.js"></script>
    <title>Home Page</title>
</head>
<body>
    <%- include('./partials/_navbar.ejs') %>
    <h1>Welcome back to the book recommendation App!</h1>
    
    <h3>An app that allows you to make a post on recommending or criticizing a book and see other people’s recommendations or critics.</h3>
    
    <form method="post" action = "/search">
        <label for="keyWordInput">Find a book:</label>
        <input type="text" id="keyWordInput" name="keyWordInput">
        <input type="submit" value="Search">
    </form>

    <br>

    <div>
        <% homepageBooks.forEach((homepageBook)=>{ %>
            <div>
                Book name: <%= homepageBook.name %>
                <br>
                Book category: <%= homepageBook.category %>
                <br>
                Book comment: <%= homepageBook.comment %>
                <br> 
                Likes: <%= homepageBook.likes.length %>
                <br> 
            </div>
            <div>
                <% if(user && homepageBook.likes.includes(user.username)) { %>
                    <form method="POST" action = "/like/<%= homepageBook.owner %>/<%= homepageBook._id%>?_method=PUT">
                    <input type="hidden" name="likes" id="likes" value="<%= user.username %>">
                    <input type="submit" value="👎">
                    </form>
                
                <% } else if (user) { %>
                    <form method="POST" action = "/like/<%= homepageBook.owner %>/<%= homepageBook._id%>">
                    <input type="hidden" name="likes" id="likes" value="<%= user.username %>">
                    <input type="submit" value="👍">
                    </form>

                <% } %>


            </div>
            <br> <br>
        <% }) %>            
    </div>

    <% if(user) { %>
        <p><a href="/users/<%= user._id%>/bookapp">Go to my book list</a></p>
    <% } %>
    
    
</body> 
</html>